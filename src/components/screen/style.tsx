//packages
import { StyleSheet } from "react-native";

//constants
import { STYLE } from "../../constants";

export default StyleSheet.create({
  Screen:{
    flex: 1,
    backgroundColor: 'white',
    padding: STYLE.padding.xl
  },
  SafeArea: {
    flex: 1
  }
});
